"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_fetch_1 = __importDefault(require("node-fetch"));
const utils_1 = require("./utils");
const enterCode = async (node) => {
    if (!node.src) {
        return;
    }
    const res = await node_fetch_1.default(node.src.url);
    const text = await res.text();
    const lines = text.split('\n');
    const startLine = node.src.startLine || 0;
    const endLine = node.src.endLine || lines.length - 1;
    node.value = lines.slice(startLine - 1, endLine).join('\n');
};
const transformer = async (tree) => {
    await utils_1.traverse(tree, {
        code: {
            enter: enterCode,
        },
    });
};
function ebastImportSourcePlugin() {
    return transformer;
}
exports.default = ebastImportSourcePlugin;
//# sourceMappingURL=import-source.js.map